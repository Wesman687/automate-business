{
  "name": "streamline-ai",
  "version": "1.0.0",
  "description": "Professional website for Streamline AI - AI and automation consulting",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && venv\\Scripts\\uvicorn.exe main:app --reload --host 127.0.0.1 --port 8001",
    "dev:full": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "install:frontend": "npm install --prefix frontend",
    "install:backend": "cd backend && python -m venv venv && venv\\Scripts\\pip.exe install -r requirements.txt",
    "setup": "npm run install:frontend && npm run install:backend",
    "lint": "npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
